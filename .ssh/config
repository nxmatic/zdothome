Include /Users/Stephane.Lacoin/.colima/ssh_config

# you should locally create a sympol link config~local to 
# the selected host configuration you're running tie:
#
#  ln -s ~/.ssh/config.d/zones.d/{name}.d/{host-name}~local config~local

include ~/.ssh/config.d/features
include ~/.ssh/config~local
