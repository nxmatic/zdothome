{
    plugin-load zi
    
    # zsh-lint (don't know if it's useful)
    zi light z-shell/zui
    zi light z-shell/zsh-lint

    # zsweep seems giving intertisting feedback
    zi param'zs_set_path' for @psprint/zsh-sweep
}
